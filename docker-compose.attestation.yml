# docker-compose.attestation.yml
# AUTO-GENERATED for 15 robot(s) — edit generate_compose.py, not this file.

services:
  robot1-sidecar:
    container_name: robot1-sidecar
    image: attestation-sidecar:latest
    environment:
      - CONFIG_PATH=/config/robot1.json
      - EXPORT_RESULTS=${EXPORT_RESULTS}
      - RESULTS_DIR=${RESULTS_DIR}
      - USE_REDIS=${USE_REDIS}
      - ONE_SHOT=${ONE_SHOT}
      - AUTO_START=${AUTO_START}
      - WAIT_FOR_TX_CONFIRMATIONS=${WAIT_FOR_TX_CONFIRMATIONS}
      - MEMORY_STORAGE_FILE=/measurements/robot1_measurements.json
      - ATTESTATION_INTERVAL_MS=${ATTESTATION_INTERVAL_MS}
      - EVENT_CONFIRMATIONS=${EVENT_CONFIRMATIONS}
      - EVENT_POLL_INTERVAL=${EVENT_POLL_INTERVAL}
      - EVENT_BATCH_SIZE=${EVENT_BATCH_SIZE}
      - LOG_LEVEL=${LOG_LEVEL}
      - ENABLE_LIBS_HASH=${ENABLE_LIBS_HASH}
      - SELF_INTEGRITY_ENABLED=${SELF_INTEGRITY_ENABLED}
    ports:
      - "8001:8000"
    volumes:
      - ./config/robot1.json:/config/robot1.json
      - ./experiments/data/:/experiments/data:rw
      - /var/run/docker.sock:/var/run/docker.sock
      - ./measurements/:/measurements:rw
      - ./checkpoints/robot1-sidecar:/checkpoints
    pid: "container:robot1"
    cap_add:
      - SYS_PTRACE
    deploy:
      resources:
        limits:
          cpus: '0.4'
    networks:
      - quorum-dev-quickstart

  robot2-sidecar:
    container_name: robot2-sidecar
    image: attestation-sidecar:latest
    environment:
      - CONFIG_PATH=/config/robot2.json
      - EXPORT_RESULTS=${EXPORT_RESULTS}
      - RESULTS_DIR=${RESULTS_DIR}
      - USE_REDIS=${USE_REDIS}
      - ONE_SHOT=${ONE_SHOT}
      - AUTO_START=${AUTO_START}
      - WAIT_FOR_TX_CONFIRMATIONS=${WAIT_FOR_TX_CONFIRMATIONS}
      - MEMORY_STORAGE_FILE=/measurements/robot2_measurements.json
      - ATTESTATION_INTERVAL_MS=${ATTESTATION_INTERVAL_MS}
      - EVENT_CONFIRMATIONS=${EVENT_CONFIRMATIONS}
      - EVENT_POLL_INTERVAL=${EVENT_POLL_INTERVAL}
      - EVENT_BATCH_SIZE=${EVENT_BATCH_SIZE}
      - LOG_LEVEL=${LOG_LEVEL}
      - ENABLE_LIBS_HASH=${ENABLE_LIBS_HASH}
      - SELF_INTEGRITY_ENABLED=${SELF_INTEGRITY_ENABLED}
    ports:
      - "8002:8000"
    volumes:
      - ./config/robot2.json:/config/robot2.json
      - ./experiments/data/:/experiments/data:rw
      - /var/run/docker.sock:/var/run/docker.sock
      - ./measurements/:/measurements:rw
      - ./checkpoints/robot2-sidecar:/checkpoints
    pid: "container:robot2"
    cap_add:
      - SYS_PTRACE
    deploy:
      resources:
        limits:
          cpus: '0.4'
    networks:
      - quorum-dev-quickstart

  robot3-sidecar:
    container_name: robot3-sidecar
    image: attestation-sidecar:latest
    environment:
      - CONFIG_PATH=/config/robot3.json
      - EXPORT_RESULTS=${EXPORT_RESULTS}
      - RESULTS_DIR=${RESULTS_DIR}
      - USE_REDIS=${USE_REDIS}
      - ONE_SHOT=${ONE_SHOT}
      - AUTO_START=${AUTO_START}
      - WAIT_FOR_TX_CONFIRMATIONS=${WAIT_FOR_TX_CONFIRMATIONS}
      - MEMORY_STORAGE_FILE=/measurements/robot3_measurements.json
      - ATTESTATION_INTERVAL_MS=${ATTESTATION_INTERVAL_MS}
      - EVENT_CONFIRMATIONS=${EVENT_CONFIRMATIONS}
      - EVENT_POLL_INTERVAL=${EVENT_POLL_INTERVAL}
      - EVENT_BATCH_SIZE=${EVENT_BATCH_SIZE}
      - LOG_LEVEL=${LOG_LEVEL}
      - ENABLE_LIBS_HASH=${ENABLE_LIBS_HASH}
      - SELF_INTEGRITY_ENABLED=${SELF_INTEGRITY_ENABLED}
    ports:
      - "8003:8000"
    volumes:
      - ./config/robot3.json:/config/robot3.json
      - ./experiments/data/:/experiments/data:rw
      - /var/run/docker.sock:/var/run/docker.sock
      - ./measurements/:/measurements:rw
      - ./checkpoints/robot3-sidecar:/checkpoints
    pid: "container:robot3"
    cap_add:
      - SYS_PTRACE
    deploy:
      resources:
        limits:
          cpus: '0.4'
    networks:
      - quorum-dev-quickstart

  robot4-sidecar:
    container_name: robot4-sidecar
    image: attestation-sidecar:latest
    environment:
      - CONFIG_PATH=/config/robot4.json
      - EXPORT_RESULTS=${EXPORT_RESULTS}
      - RESULTS_DIR=${RESULTS_DIR}
      - USE_REDIS=${USE_REDIS}
      - ONE_SHOT=${ONE_SHOT}
      - AUTO_START=${AUTO_START}
      - WAIT_FOR_TX_CONFIRMATIONS=${WAIT_FOR_TX_CONFIRMATIONS}
      - MEMORY_STORAGE_FILE=/measurements/robot4_measurements.json
      - ATTESTATION_INTERVAL_MS=${ATTESTATION_INTERVAL_MS}
      - EVENT_CONFIRMATIONS=${EVENT_CONFIRMATIONS}
      - EVENT_POLL_INTERVAL=${EVENT_POLL_INTERVAL}
      - EVENT_BATCH_SIZE=${EVENT_BATCH_SIZE}
      - LOG_LEVEL=${LOG_LEVEL}
      - ENABLE_LIBS_HASH=${ENABLE_LIBS_HASH}
      - SELF_INTEGRITY_ENABLED=${SELF_INTEGRITY_ENABLED}
    ports:
      - "8004:8000"
    volumes:
      - ./config/robot4.json:/config/robot4.json
      - ./experiments/data/:/experiments/data:rw
      - /var/run/docker.sock:/var/run/docker.sock
      - ./measurements/:/measurements:rw
      - ./checkpoints/robot4-sidecar:/checkpoints
    pid: "container:robot4"
    cap_add:
      - SYS_PTRACE
    deploy:
      resources:
        limits:
          cpus: '0.4'
    networks:
      - quorum-dev-quickstart

  robot5-sidecar:
    container_name: robot5-sidecar
    image: attestation-sidecar:latest
    environment:
      - CONFIG_PATH=/config/robot5.json
      - EXPORT_RESULTS=${EXPORT_RESULTS}
      - RESULTS_DIR=${RESULTS_DIR}
      - USE_REDIS=${USE_REDIS}
      - ONE_SHOT=${ONE_SHOT}
      - AUTO_START=${AUTO_START}
      - WAIT_FOR_TX_CONFIRMATIONS=${WAIT_FOR_TX_CONFIRMATIONS}
      - MEMORY_STORAGE_FILE=/measurements/robot5_measurements.json
      - ATTESTATION_INTERVAL_MS=${ATTESTATION_INTERVAL_MS}
      - EVENT_CONFIRMATIONS=${EVENT_CONFIRMATIONS}
      - EVENT_POLL_INTERVAL=${EVENT_POLL_INTERVAL}
      - EVENT_BATCH_SIZE=${EVENT_BATCH_SIZE}
      - LOG_LEVEL=${LOG_LEVEL}
      - ENABLE_LIBS_HASH=${ENABLE_LIBS_HASH}
      - SELF_INTEGRITY_ENABLED=${SELF_INTEGRITY_ENABLED}
    ports:
      - "8005:8000"
    volumes:
      - ./config/robot5.json:/config/robot5.json
      - ./experiments/data/:/experiments/data:rw
      - /var/run/docker.sock:/var/run/docker.sock
      - ./measurements/:/measurements:rw
      - ./checkpoints/robot5-sidecar:/checkpoints
    pid: "container:robot5"
    cap_add:
      - SYS_PTRACE
    deploy:
      resources:
        limits:
          cpus: '0.4'
    networks:
      - quorum-dev-quickstart

  robot6-sidecar:
    container_name: robot6-sidecar
    image: attestation-sidecar:latest
    environment:
      - CONFIG_PATH=/config/robot6.json
      - EXPORT_RESULTS=${EXPORT_RESULTS}
      - RESULTS_DIR=${RESULTS_DIR}
      - USE_REDIS=${USE_REDIS}
      - ONE_SHOT=${ONE_SHOT}
      - AUTO_START=${AUTO_START}
      - WAIT_FOR_TX_CONFIRMATIONS=${WAIT_FOR_TX_CONFIRMATIONS}
      - MEMORY_STORAGE_FILE=/measurements/robot6_measurements.json
      - ATTESTATION_INTERVAL_MS=${ATTESTATION_INTERVAL_MS}
      - EVENT_CONFIRMATIONS=${EVENT_CONFIRMATIONS}
      - EVENT_POLL_INTERVAL=${EVENT_POLL_INTERVAL}
      - EVENT_BATCH_SIZE=${EVENT_BATCH_SIZE}
      - LOG_LEVEL=${LOG_LEVEL}
      - ENABLE_LIBS_HASH=${ENABLE_LIBS_HASH}
      - SELF_INTEGRITY_ENABLED=${SELF_INTEGRITY_ENABLED}
    ports:
      - "8006:8000"
    volumes:
      - ./config/robot6.json:/config/robot6.json
      - ./experiments/data/:/experiments/data:rw
      - /var/run/docker.sock:/var/run/docker.sock
      - ./measurements/:/measurements:rw
      - ./checkpoints/robot6-sidecar:/checkpoints
    pid: "container:robot6"
    cap_add:
      - SYS_PTRACE
    deploy:
      resources:
        limits:
          cpus: '0.4'
    networks:
      - quorum-dev-quickstart

  robot7-sidecar:
    container_name: robot7-sidecar
    image: attestation-sidecar:latest
    environment:
      - CONFIG_PATH=/config/robot7.json
      - EXPORT_RESULTS=${EXPORT_RESULTS}
      - RESULTS_DIR=${RESULTS_DIR}
      - USE_REDIS=${USE_REDIS}
      - ONE_SHOT=${ONE_SHOT}
      - AUTO_START=${AUTO_START}
      - WAIT_FOR_TX_CONFIRMATIONS=${WAIT_FOR_TX_CONFIRMATIONS}
      - MEMORY_STORAGE_FILE=/measurements/robot7_measurements.json
      - ATTESTATION_INTERVAL_MS=${ATTESTATION_INTERVAL_MS}
      - EVENT_CONFIRMATIONS=${EVENT_CONFIRMATIONS}
      - EVENT_POLL_INTERVAL=${EVENT_POLL_INTERVAL}
      - EVENT_BATCH_SIZE=${EVENT_BATCH_SIZE}
      - LOG_LEVEL=${LOG_LEVEL}
      - ENABLE_LIBS_HASH=${ENABLE_LIBS_HASH}
      - SELF_INTEGRITY_ENABLED=${SELF_INTEGRITY_ENABLED}
    ports:
      - "8007:8000"
    volumes:
      - ./config/robot7.json:/config/robot7.json
      - ./experiments/data/:/experiments/data:rw
      - /var/run/docker.sock:/var/run/docker.sock
      - ./measurements/:/measurements:rw
      - ./checkpoints/robot7-sidecar:/checkpoints
    pid: "container:robot7"
    cap_add:
      - SYS_PTRACE
    deploy:
      resources:
        limits:
          cpus: '0.4'
    networks:
      - quorum-dev-quickstart

  robot8-sidecar:
    container_name: robot8-sidecar
    image: attestation-sidecar:latest
    environment:
      - CONFIG_PATH=/config/robot8.json
      - EXPORT_RESULTS=${EXPORT_RESULTS}
      - RESULTS_DIR=${RESULTS_DIR}
      - USE_REDIS=${USE_REDIS}
      - ONE_SHOT=${ONE_SHOT}
      - AUTO_START=${AUTO_START}
      - WAIT_FOR_TX_CONFIRMATIONS=${WAIT_FOR_TX_CONFIRMATIONS}
      - MEMORY_STORAGE_FILE=/measurements/robot8_measurements.json
      - ATTESTATION_INTERVAL_MS=${ATTESTATION_INTERVAL_MS}
      - EVENT_CONFIRMATIONS=${EVENT_CONFIRMATIONS}
      - EVENT_POLL_INTERVAL=${EVENT_POLL_INTERVAL}
      - EVENT_BATCH_SIZE=${EVENT_BATCH_SIZE}
      - LOG_LEVEL=${LOG_LEVEL}
      - ENABLE_LIBS_HASH=${ENABLE_LIBS_HASH}
      - SELF_INTEGRITY_ENABLED=${SELF_INTEGRITY_ENABLED}
    ports:
      - "8008:8000"
    volumes:
      - ./config/robot8.json:/config/robot8.json
      - ./experiments/data/:/experiments/data:rw
      - /var/run/docker.sock:/var/run/docker.sock
      - ./measurements/:/measurements:rw
      - ./checkpoints/robot8-sidecar:/checkpoints
    pid: "container:robot8"
    cap_add:
      - SYS_PTRACE
    deploy:
      resources:
        limits:
          cpus: '0.4'
    networks:
      - quorum-dev-quickstart

  robot9-sidecar:
    container_name: robot9-sidecar
    image: attestation-sidecar:latest
    environment:
      - CONFIG_PATH=/config/robot9.json
      - EXPORT_RESULTS=${EXPORT_RESULTS}
      - RESULTS_DIR=${RESULTS_DIR}
      - USE_REDIS=${USE_REDIS}
      - ONE_SHOT=${ONE_SHOT}
      - AUTO_START=${AUTO_START}
      - WAIT_FOR_TX_CONFIRMATIONS=${WAIT_FOR_TX_CONFIRMATIONS}
      - MEMORY_STORAGE_FILE=/measurements/robot9_measurements.json
      - ATTESTATION_INTERVAL_MS=${ATTESTATION_INTERVAL_MS}
      - EVENT_CONFIRMATIONS=${EVENT_CONFIRMATIONS}
      - EVENT_POLL_INTERVAL=${EVENT_POLL_INTERVAL}
      - EVENT_BATCH_SIZE=${EVENT_BATCH_SIZE}
      - LOG_LEVEL=${LOG_LEVEL}
      - ENABLE_LIBS_HASH=${ENABLE_LIBS_HASH}
      - SELF_INTEGRITY_ENABLED=${SELF_INTEGRITY_ENABLED}
    ports:
      - "8009:8000"
    volumes:
      - ./config/robot9.json:/config/robot9.json
      - ./experiments/data/:/experiments/data:rw
      - /var/run/docker.sock:/var/run/docker.sock
      - ./measurements/:/measurements:rw
      - ./checkpoints/robot9-sidecar:/checkpoints
    pid: "container:robot9"
    cap_add:
      - SYS_PTRACE
    deploy:
      resources:
        limits:
          cpus: '0.4'
    networks:
      - quorum-dev-quickstart

  robot10-sidecar:
    container_name: robot10-sidecar
    image: attestation-sidecar:latest
    environment:
      - CONFIG_PATH=/config/robot10.json
      - EXPORT_RESULTS=${EXPORT_RESULTS}
      - RESULTS_DIR=${RESULTS_DIR}
      - USE_REDIS=${USE_REDIS}
      - ONE_SHOT=${ONE_SHOT}
      - AUTO_START=${AUTO_START}
      - WAIT_FOR_TX_CONFIRMATIONS=${WAIT_FOR_TX_CONFIRMATIONS}
      - MEMORY_STORAGE_FILE=/measurements/robot10_measurements.json
      - ATTESTATION_INTERVAL_MS=${ATTESTATION_INTERVAL_MS}
      - EVENT_CONFIRMATIONS=${EVENT_CONFIRMATIONS}
      - EVENT_POLL_INTERVAL=${EVENT_POLL_INTERVAL}
      - EVENT_BATCH_SIZE=${EVENT_BATCH_SIZE}
      - LOG_LEVEL=${LOG_LEVEL}
      - ENABLE_LIBS_HASH=${ENABLE_LIBS_HASH}
      - SELF_INTEGRITY_ENABLED=${SELF_INTEGRITY_ENABLED}
    ports:
      - "8010:8000"
    volumes:
      - ./config/robot10.json:/config/robot10.json
      - ./experiments/data/:/experiments/data:rw
      - /var/run/docker.sock:/var/run/docker.sock
      - ./measurements/:/measurements:rw
      - ./checkpoints/robot10-sidecar:/checkpoints
    pid: "container:robot10"
    cap_add:
      - SYS_PTRACE
    deploy:
      resources:
        limits:
          cpus: '0.4'
    networks:
      - quorum-dev-quickstart

  robot11-sidecar:
    container_name: robot11-sidecar
    image: attestation-sidecar:latest
    environment:
      - CONFIG_PATH=/config/robot11.json
      - EXPORT_RESULTS=${EXPORT_RESULTS}
      - RESULTS_DIR=${RESULTS_DIR}
      - USE_REDIS=${USE_REDIS}
      - ONE_SHOT=${ONE_SHOT}
      - AUTO_START=${AUTO_START}
      - WAIT_FOR_TX_CONFIRMATIONS=${WAIT_FOR_TX_CONFIRMATIONS}
      - MEMORY_STORAGE_FILE=/measurements/robot11_measurements.json
      - ATTESTATION_INTERVAL_MS=${ATTESTATION_INTERVAL_MS}
      - EVENT_CONFIRMATIONS=${EVENT_CONFIRMATIONS}
      - EVENT_POLL_INTERVAL=${EVENT_POLL_INTERVAL}
      - EVENT_BATCH_SIZE=${EVENT_BATCH_SIZE}
      - LOG_LEVEL=${LOG_LEVEL}
      - ENABLE_LIBS_HASH=${ENABLE_LIBS_HASH}
      - SELF_INTEGRITY_ENABLED=${SELF_INTEGRITY_ENABLED}
    ports:
      - "8011:8000"
    volumes:
      - ./config/robot11.json:/config/robot11.json
      - ./experiments/data/:/experiments/data:rw
      - /var/run/docker.sock:/var/run/docker.sock
      - ./measurements/:/measurements:rw
      - ./checkpoints/robot11-sidecar:/checkpoints
    pid: "container:robot11"
    cap_add:
      - SYS_PTRACE
    deploy:
      resources:
        limits:
          cpus: '0.4'
    networks:
      - quorum-dev-quickstart

  robot12-sidecar:
    container_name: robot12-sidecar
    image: attestation-sidecar:latest
    environment:
      - CONFIG_PATH=/config/robot12.json
      - EXPORT_RESULTS=${EXPORT_RESULTS}
      - RESULTS_DIR=${RESULTS_DIR}
      - USE_REDIS=${USE_REDIS}
      - ONE_SHOT=${ONE_SHOT}
      - AUTO_START=${AUTO_START}
      - WAIT_FOR_TX_CONFIRMATIONS=${WAIT_FOR_TX_CONFIRMATIONS}
      - MEMORY_STORAGE_FILE=/measurements/robot12_measurements.json
      - ATTESTATION_INTERVAL_MS=${ATTESTATION_INTERVAL_MS}
      - EVENT_CONFIRMATIONS=${EVENT_CONFIRMATIONS}
      - EVENT_POLL_INTERVAL=${EVENT_POLL_INTERVAL}
      - EVENT_BATCH_SIZE=${EVENT_BATCH_SIZE}
      - LOG_LEVEL=${LOG_LEVEL}
      - ENABLE_LIBS_HASH=${ENABLE_LIBS_HASH}
      - SELF_INTEGRITY_ENABLED=${SELF_INTEGRITY_ENABLED}
    ports:
      - "8012:8000"
    volumes:
      - ./config/robot12.json:/config/robot12.json
      - ./experiments/data/:/experiments/data:rw
      - /var/run/docker.sock:/var/run/docker.sock
      - ./measurements/:/measurements:rw
      - ./checkpoints/robot12-sidecar:/checkpoints
    pid: "container:robot12"
    cap_add:
      - SYS_PTRACE
    deploy:
      resources:
        limits:
          cpus: '0.4'
    networks:
      - quorum-dev-quickstart

  robot13-sidecar:
    container_name: robot13-sidecar
    image: attestation-sidecar:latest
    environment:
      - CONFIG_PATH=/config/robot13.json
      - EXPORT_RESULTS=${EXPORT_RESULTS}
      - RESULTS_DIR=${RESULTS_DIR}
      - USE_REDIS=${USE_REDIS}
      - ONE_SHOT=${ONE_SHOT}
      - AUTO_START=${AUTO_START}
      - WAIT_FOR_TX_CONFIRMATIONS=${WAIT_FOR_TX_CONFIRMATIONS}
      - MEMORY_STORAGE_FILE=/measurements/robot13_measurements.json
      - ATTESTATION_INTERVAL_MS=${ATTESTATION_INTERVAL_MS}
      - EVENT_CONFIRMATIONS=${EVENT_CONFIRMATIONS}
      - EVENT_POLL_INTERVAL=${EVENT_POLL_INTERVAL}
      - EVENT_BATCH_SIZE=${EVENT_BATCH_SIZE}
      - LOG_LEVEL=${LOG_LEVEL}
      - ENABLE_LIBS_HASH=${ENABLE_LIBS_HASH}
      - SELF_INTEGRITY_ENABLED=${SELF_INTEGRITY_ENABLED}
    ports:
      - "8013:8000"
    volumes:
      - ./config/robot13.json:/config/robot13.json
      - ./experiments/data/:/experiments/data:rw
      - /var/run/docker.sock:/var/run/docker.sock
      - ./measurements/:/measurements:rw
      - ./checkpoints/robot13-sidecar:/checkpoints
    pid: "container:robot13"
    cap_add:
      - SYS_PTRACE
    deploy:
      resources:
        limits:
          cpus: '0.4'
    networks:
      - quorum-dev-quickstart

  robot14-sidecar:
    container_name: robot14-sidecar
    image: attestation-sidecar:latest
    environment:
      - CONFIG_PATH=/config/robot14.json
      - EXPORT_RESULTS=${EXPORT_RESULTS}
      - RESULTS_DIR=${RESULTS_DIR}
      - USE_REDIS=${USE_REDIS}
      - ONE_SHOT=${ONE_SHOT}
      - AUTO_START=${AUTO_START}
      - WAIT_FOR_TX_CONFIRMATIONS=${WAIT_FOR_TX_CONFIRMATIONS}
      - MEMORY_STORAGE_FILE=/measurements/robot14_measurements.json
      - ATTESTATION_INTERVAL_MS=${ATTESTATION_INTERVAL_MS}
      - EVENT_CONFIRMATIONS=${EVENT_CONFIRMATIONS}
      - EVENT_POLL_INTERVAL=${EVENT_POLL_INTERVAL}
      - EVENT_BATCH_SIZE=${EVENT_BATCH_SIZE}
      - LOG_LEVEL=${LOG_LEVEL}
      - ENABLE_LIBS_HASH=${ENABLE_LIBS_HASH}
      - SELF_INTEGRITY_ENABLED=${SELF_INTEGRITY_ENABLED}
    ports:
      - "8014:8000"
    volumes:
      - ./config/robot14.json:/config/robot14.json
      - ./experiments/data/:/experiments/data:rw
      - /var/run/docker.sock:/var/run/docker.sock
      - ./measurements/:/measurements:rw
      - ./checkpoints/robot14-sidecar:/checkpoints
    pid: "container:robot14"
    cap_add:
      - SYS_PTRACE
    deploy:
      resources:
        limits:
          cpus: '0.4'
    networks:
      - quorum-dev-quickstart

  robot15-sidecar:
    container_name: robot15-sidecar
    image: attestation-sidecar:latest
    environment:
      - CONFIG_PATH=/config/robot15.json
      - EXPORT_RESULTS=${EXPORT_RESULTS}
      - RESULTS_DIR=${RESULTS_DIR}
      - USE_REDIS=${USE_REDIS}
      - ONE_SHOT=${ONE_SHOT}
      - AUTO_START=${AUTO_START}
      - WAIT_FOR_TX_CONFIRMATIONS=${WAIT_FOR_TX_CONFIRMATIONS}
      - MEMORY_STORAGE_FILE=/measurements/robot15_measurements.json
      - ATTESTATION_INTERVAL_MS=${ATTESTATION_INTERVAL_MS}
      - EVENT_CONFIRMATIONS=${EVENT_CONFIRMATIONS}
      - EVENT_POLL_INTERVAL=${EVENT_POLL_INTERVAL}
      - EVENT_BATCH_SIZE=${EVENT_BATCH_SIZE}
      - LOG_LEVEL=${LOG_LEVEL}
      - ENABLE_LIBS_HASH=${ENABLE_LIBS_HASH}
      - SELF_INTEGRITY_ENABLED=${SELF_INTEGRITY_ENABLED}
    ports:
      - "8015:8000"
    volumes:
      - ./config/robot15.json:/config/robot15.json
      - ./experiments/data/:/experiments/data:rw
      - /var/run/docker.sock:/var/run/docker.sock
      - ./measurements/:/measurements:rw
      - ./checkpoints/robot15-sidecar:/checkpoints
    pid: "container:robot15"
    cap_add:
      - SYS_PTRACE
    deploy:
      resources:
        limits:
          cpus: '0.4'
    networks:
      - quorum-dev-quickstart

networks:
  quorum-dev-quickstart:
    external: true
